Public libraries(s) go here

I.e. library files that when installed go into either /usr/include or
/user/local/include.

Note: this directory is a container for local builds. Besides this README,
scm should be empty.
